import Component from '@ember/component';
import layout from '../../templates/components/nypr-account-modal/footer';

export default Component.extend({
  classNames: ['nypr-account-modal-footer'],
  layout
});
