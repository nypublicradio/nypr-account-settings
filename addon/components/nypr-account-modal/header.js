import Component from '@ember/component';
import layout from '../../templates/components/nypr-account-modal/header';

export default Component.extend({
  classNames: ['nypr-account-modal-title'],
  layout,

  closeAction() {}
});
