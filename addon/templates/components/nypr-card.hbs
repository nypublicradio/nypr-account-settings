<div class="nypr-card-header">
  <span class="nypr-title">{{title}}</span>
  
  {{#if edit}}
  <button class="nypr-edit-btn" {{action (action edit)}} data-test-selector="edit-button">
    Edit info
  </button>
  {{/if}}
</div> 

{{yield}}
