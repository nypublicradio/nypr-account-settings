machine:
  node:
    version: 6.9.2

dependencies:
  pre:
    - sudo apt-get update && sudo apt-get install ruby-sass
    - gem update --system
    - gem install compass --no-ri --no-rdoc
    - npm install -g bower
    - bower install
